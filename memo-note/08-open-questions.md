# 08 · 待定问题

最后更新：R3（2026-09-17）
本轮变化：Q-01、Q-02、Q-08 转为 answered 并移入下方已答表；新增 Q-20、Q-21。

状态：`open` 待用户回答 · `answered` 已回答（摘要见备注）· `deferred` 用户明确先不答 · `analysis` 需要我做进一步分析后再问

| 编号 | 问题 | 状态 | 轮次 | 备注 / 回答摘要 |
|---|---|---|---|---|
| Q-03 | critic 轮是 move on 的必经步骤还是可选？ | open | R2 新增 | 与 D-08 相关 |
| Q-04 | 分批 fire 的默认粒度（按场次 / 镜头组 / 全部）？ | open | R2 新增 | |
| Q-05 | 节点 active 时，同阶段其他节点如何缩排、上游列如何折叠？ | analysis | R2 新增 | 我可先出线框再问 |
| Q-06 | 下钻的快捷路径（双击 / 回车）是否保留二次确认？ | open | R1 | 目标用户是懂行朋友，仪式感可留但要有快捷路径 |
| Q-07 | 独立 critic skill 何时设计？先做通用一份还是每工种一份？ | deferred | R2 | 用户说"后续" |
| Q-09 | 段落级"需复核"第一版做不做，还是先整份工件标 stale？ | open | R2 新增 | D-18 的退回请求带具体段落，倾向第一版就要段落 / 镜头 ID 级 |
| Q-10 | 成本归因粒度：按节点还是按 turn？ | open | R2 新增 | D-07 |
| Q-11 | 两次扳机用词：pull the trigger（散开）与 fire（生成）是否改一个？ | open | R1 | D-14 说用词非硬性 |
| Q-12 | agent 运行时：codex app-server / Claude Agent SDK / 自写薄循环？ | open | R1→R2 | 用户倾向 codex；06-A 有映射表与注意点 |
| Q-13 | 客户端平台：Electron / Tauri / Swift（+WKWebView 混合）？ | open | R1→R2 | 用户说短期 Swift 也可接受；06-B 有对比 |
| Q-14 | 剪辑引擎组合：ffmpeg 组装 + HyperFrames 图形 + 哪个时间线 UI？ | open | R2 | 06-C 有调研与初步方向 |
| Q-15 | 生成 provider 名单（视频 / 图 / 音乐）与 API 权限？ | open | R1 | 决定能力表第一批内容 |
| Q-16 | 制片作为 HUD、场记作为进度条 / 台账层——确认这个呈现？ | open | R1 建议 | 用户 R2 未反对 |
| Q-17 | 音乐生成服务选哪家？音乐 brief 由声音导演节点直接产出吗？ | open | R2 新增 | D-09 |
| Q-18 | 视频生成时随片生成的配音 / 音效，若某镜头效果不好，是否允许该镜头改走独立 TTS？ | open | R2 新增 | 数据模型预留了 TTS 轨 |
| Q-19 | "9 个 skill 之外还需要更多 skill、数据库、数据内容"——02 的缺口清单是否符合你的设想？有缺漏吗？ | open | R2 新增 | |
| Q-20 | 资源库读取权限：你点名了制片 / 导演 / 美术；角色与服化（角色参考）、摄影（光线参考）、编译（分配参考职责）、审片（比对参考帧）是否也开放？ | open | R3 新增 | 我的建议是全部可读，写入受限；见 05 |
| Q-21 | 合并裁决节点是否默认执行 skill 里的"多岗逐镜审稿"表（各岗对镜头 1..N 逐行结论），还是只在用户点名或进入批量生成前开启？ | open | R3 新增 | skill 原文默认不打印；每镜 × 三岗成本可观 |

## 已回答

| 问题 | 回答 | 轮次 |
|---|---|---|
| Q-01 第一张纸上的"9"是工种任务单还是创意方向？ | 工种任务单；先对齐初步需求，让上下文区隔的子 agent 有共同方向 → D-16 | R3 |
| Q-02 是否要"导演合并裁决"节点？ | 需要，在美术 / 角色 / 摄影 ready 后、编译前 → D-18 | R3 |
| Q-08 参考图 / 品牌素材从哪一步进入？ | 初步沟通时引导注入，存项目资源库，制片 / 导演 / 美术可取用 → D-17 | R3 |
| 只做 AI 生成还是也覆盖实拍？ | 只做生成 → D-01 | R2 |
| 新描述取代还是叠在旧方案上？ | 旧方案忽略，按需参考 → D-11 | R2 |
| 谁当 Critic？ | 对应 agent 自己承担，后续设计独立 critic skill → D-08 | R2 |
| 声音 v1 生成还是只导出方案？ | 音乐用生成服务；配音音效写进视频提示 → D-09 | R2 |
| 历史任务 = 一条视频？允许分叉？ | 一整条工作流轮次含所有分支 → D-10 | R2 |
| 中英混合是否刻意？ | 趣味性，非硬性 → D-14 | R2 |
| 目标用户？ | 工作室内的朋友 → D-13 | R2 |
| 平台？ | 未定，短期 Swift 可接受 → Q-13 保留 | R2 |
