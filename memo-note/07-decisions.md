# 07 · 已定决策

最后更新：R3（2026-09-17）
本轮变化：新增 D-16 ~ D-18。

| 编号 | 决策 | 轮次 | 依据 / 备注 |
|---|---|---|---|
| D-01 | 只做 AI 生成视频，不做实拍策划 | R2 | 用户 R2-8 |
| D-02 | 9 条线不同时射出；采用"同时出现、部分先冻结、上游 ready 后解冻"的方式，解冻带真实进度的加载动画 | R2 | 用户 R2-2；分波射出作为备选放弃 |
| D-03 | 工作流是自动展开的节点式（ComfyUI 类比），用户不手动连线；每个节点给对应 agent 的聊天入口 | R2 | 用户 R2-6 |
| D-04 | 节点内由 agent 在信息足够时调用"确认需求工具"，用户 yes / no / input 决定提交或继续勾兑 | R2 | 用户 R2-6 |
| D-05 | 回到历史节点时提供 branch 或从当前节点刷新覆盖两种操作 | R2 | 用户 R2-6 |
| D-06 | 文档右上角有展开聊天按钮，聊天区从右侧拉出显示该节点历史 | R2 | 用户 R2-6 |
| D-07 | 制片重定义：不做资源与排期；预算 = token + 生图 + 生音乐 + 生视频的成本核算与估算 | R2 | 用户 R2-1 |
| D-08 | 每个分支 agent 的 critic 先由对应 agent 自己承担；独立 critic skill 后续设计 | R2 | 用户 R2-9 |
| D-09 | 音乐由音乐生成服务生成；配音与音效写进视频生成提示词 | R2 | 用户 R2-10 |
| D-10 | 历史任务 = 一整条工作流轮次的全部内容，含其中所有分支 | R2 | 用户 R2-11 |
| D-11 | 旧的 7 席位 studio 方案与 Matrix dossier 不作为基线，仅按需参考 | R2 | 用户 R2-5 |
| D-12 | 剪辑能力范围：基础剪辑 + 部分图形制作；专业调色通过导出工程交给 PR / 达芬奇 | R2 | 用户 R2-7 |
| D-13 | 目标用户为工作室内的朋友（懂行的小群体） | R2 | 用户 R2-13 |
| D-14 | 中英混合界面用词是趣味性选择，非硬性要求 | R2 | 用户 R2-12 |
| D-15 | 讨论记录以 memo-note 目录下结构化文档形式维护，每轮更新 | R2 | 用户 R2 开头 |
| D-16 | 第一张纸上的 9 行是**工种任务单**；先对齐用户初步需求，让上下文区隔的各子 agent 有共同方向 | R3 | 用户 R3；原则见 02 / 03 |
| D-17 | 参考图与品牌素材在**初步沟通阶段引导用户注入**，存入项目资源库；制片、导演、美术可取用，需要的地方都能调用 | R3 | 用户 R3；其他角色的读取权限见 Q-20 |
| D-18 | 美术 / 角色 / 摄影三者 ready 后、进入编译前，设**导演合并裁决**节点 | R3 | 用户 R3；节点定义见 03 |

## 用户表达了倾向但未定案

- codex 开源 harness 作为基座（用户 R2-4："似乎可以"，"具体实现还没想好"）→ Q-12。
- 制片 / 场记不作为文件夹而作为 HUD / 进度条（R1 建议，用户 R2 未反对也未确认）→ Q-16。
