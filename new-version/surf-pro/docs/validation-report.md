# 发布验证报告 · Surf Pro 2.0.0

核验日期：2026-09-18。测试环境为本次工作容器；不是用户设备或桌面端。

## 实际运行的检查

| 检查 | 结果 | 能说明什么 |
|---|---|---|
| Python 离线测试 | 55 项通过，0 失败，0 跳过。原始日志见 `offline-tests.txt`。 | 校验器在这些合成输入上的结构规则、异常处理、路径与哈希规则符合测试预期。 |
| 空白研究记录模板 | CLI 返回 0；有教学模板提示。 | 模板结构有效、状态为 draft；不是实际调研完成。 |
| 入口和 UI YAML | 解析通过；name 与 surf-pro 目录一致，描述与 metadata 类型符合检查条件。 | 包结构具备明确入口；不是所有宿主兼容性认证。 |
| 相对链接与文件语法 | 本地 Markdown 链接目标存在；Python 可解析；JSON 可解析。 | 无已检测到的缺失引用文件或语法问题。 |
| 行为案例定义 | 30 个唯一编号；全部标为 not_run。 | 有可执行的验收定义，但没有行为成绩。 |
| 发布压缩包 | 完整性、单一根目录、路径安全和逐文件 SHA-256 复核通过；解压后重新运行测试与模板检查通过。 | 压缩包能完整还原本次交付，不含 macOS/缓存垃圾。 |

离线测试覆盖：单一直接来源可支持窄事实；摘要/搜索片段不能冒充足够原文；共同原始出处警告；状态与反证冲突；问题与结论关联；延展关闭、直接锚定与升级前提；部分结果合法完成；无效日期/字段/类型；素材缺失、哈希不符、路径穿越和符号链接逃逸；命令行返回码。

这些测试未使用真实产品或价格数据。示例中的合成来源域名未被访问，不能作为任何外部事实的依据。异常输入使用参数化子测试，未把每个变体重复计为独立研究案例。

复核命令，在 skill 根目录执行：

```bash
python -m unittest discover -s tests -v
python scripts/validate_research.py templates/research-record.json
```

`MANIFEST.sha256` 记录发布文件哈希，不包括清单自身。哈希保证文件一致性，不保证内容真实或适合任意任务。

## 未执行，不能声称通过

未在用户的 ChatGPT/Codex 客户端导入；未验证特定桌面版本的 ZIP 安装 UI；未运行 30 个宿主行为案例；未跑新版与旧版的真实开放检索对照；未测准确率、召回率、引用语义正确率、省时百分比或用户满意度；未启动监控；未调用用户付费服务。

参考资料已按 `references/08-methodology-sources.md` 所述范围读取，但不是系统文献综述，也不是用学术引用替代产品评测。

## 使用时仍需复核

研究资料必须真实读取；引用必须实际支持对应陈述；延展必须符合当前用户目标；重要动态事实要更新。脚本不能识别任意自然语言中的事实错误，也不能验证“已看图片”等声明是否真实、来源是否真独立或版权是否允许。

本次能确认的是包与结构检查结果，不能保证 skill 在不同模型、工具和权限环境中必然得到同样质量。需要行为验证时，按 `evals/README.md` 运行并保留失败记录。
