# 实际验证报告

构建与检查日期：2026-09-18。范围：本地文件封装、确定性工具与合成测试。没有在用户设备或真实宿主安装。

## 已执行

| 检查 | 结果 | 说明 |
|---|---|---|
| 单一入口与元数据 | 通过 | 一个根目录、一个 SKILL.md；name/description 与中文界面文件存在。 |
| 内部文件链接 | 通过 | 相对路径目标可解析且不越过技能目录；不声称外部网页和页内锚点全部有效。 |
| 知识完整性 | 通过 | 两套知识共 303 份；原始与封装后 SHA-256 分别核对。 |
| 单元测试 | 56 项通过 | 见实际 [运行日志](tests/last-run.txt)，无失败。 |
| 隐藏杂项与符号链接 | 通过 | 排除 .DS_Store、__MACOSX、._、__pycache__，不打包符号链接。 |

其中 41 项针对批准/来源/哈希/场次/精确文本等交付合同，15 项针对封装。另实际运行了交付 CLI 的合成配对、哈希计算与未填写模板拒绝检查，见 [CLI 记录](tests/cli-smoke-result.json)。合成批准仅存在于临时测试夹，不能用于真实项目。

[来源字节检查记录](tests/source-verification.json) 保存本次实际核对范围；[知识来源清单](references/source-manifest.json) 列出所有原路径及两种哈希。本包保留 248 处未随原 ZIP 附带的历史定位说明；这是出现次数，不等于缺失文件数量。没有把不存在的旧材料补写成真实来源。

## 未执行，不能算通过

24 个 [行为评测用例](evals/cases.json) 均为 `NOT_RUN`：尚未在实际宿主验证自动触发、跨技能选择、长对话持续修改与用户确认行为。知识引用的全量学术复核、外部链接重查、原视频核验、真实模型适配、声画生成与受众效果也未执行。

Skill 本文给出规程，不保证模型永不出错。字段和哈希通过不证明用户确认真实，不证明剧情/行动主体/信息时机等价，不证明审美效果或制作成功。

还特意测试了一个限制：仅改未标记动作主体、同步更新派生文件哈希后，静态程序可以结构通过，但会列出语义未验证。这个用例证明必须另外阅读行动关系和信息时机，**不是证明错误剧情也符合交付要求**。

## 如何复查

```bash
PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -v
python scripts/check_package.py .
```

命令从本技能根目录执行，Python 3.9+，无外部 Python 依赖。未实际运行的环境请如实标未运行。源文件若有合法更新，应同时更新来源说明和清单，而不是保留旧“通过”结论。
